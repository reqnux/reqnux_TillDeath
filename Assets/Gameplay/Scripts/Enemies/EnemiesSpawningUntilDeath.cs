using UnityEngine;
using System.Collections;

public class EnemiesSpawningUntilDeath : MonoBehaviour {

	//MissionEvents with SpawnType == UNTIL_ENEMY_DEATH will be active until
	//at least one instance of this script will be active
}
